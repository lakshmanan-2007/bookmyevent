spring:
  application:
    name: bookmyevent
  # Default profile uses in-memory H2 so the app runs with zero setup.
  profiles:
    active: dev
  # Redis is only used when app.lock.provider=redis. Lettuce connects lazily,
  # so this default is harmless when no Redis server is present.
  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

server:
  # Cloud hosts (Render/Railway) inject PORT; fall back to 8080 locally.
  port: ${PORT:8080}

app:
  jwt:
    # Base64-encoded 256-bit secret. Override in production via APP_JWT_SECRET env var.
    secret: ${APP_JWT_SECRET:Y2hhbmdlLW1lLXRoaXMtaXMtYS1kZXYtb25seS1zZWNyZXQta2V5LTEyMw==}
    expiration-ms: 86400000   # 24h
  cors:
    # Comma-separated allowed origins. In prod set APP_CORS_ORIGINS to your frontend URL (or * for any).
    allowed-origins: ${APP_CORS_ORIGINS:http://localhost:3000}
  lock:
    # 'db'    = single instance, uses the DB pessimistic lock (default, no Redis needed).
    # 'redis' = distributed lock, oversell-safe across many instances (needs Redis).
    provider: ${APP_LOCK_PROVIDER:db}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: never

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:bookmyevent;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;LOCK_TIMEOUT=10000
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate.format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: never

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${JDBC_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bookmyevent}}
    username: ${DB_USER:bookmyevent}
    password: ${DB_PASSWORD:bookmyevent}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
